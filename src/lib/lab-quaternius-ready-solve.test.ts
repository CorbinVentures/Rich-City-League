import {describe,it,expect} from 'vitest';import fs from 'node:fs';describe('real-character stance solve gate',()=>{it('measures solved joints rather than target intentions',()=>{const s=fs.readFileSync('scripts/solve-quaternius-ready-stance.mjs','utf8');for(const x of ['leftKneeAngleDeg','rightKneeAngleDeg','leftElbowAngleDeg','leftKneeClearance','leftFootError','straight-knee','t-pose','foot-lock'])expect(s).toContain(x);expect(s).toContain('if(failures.length)process.exit(1)')});});