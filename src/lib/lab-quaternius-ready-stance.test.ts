import {describe,it,expect} from 'vitest';import fs from 'node:fs';describe('real Quaternius stance target builder',()=>{it('derives targets only from measured report',()=>{const s=fs.readFileSync('scripts/build-quaternius-ready-stance.mjs','utf8');for(const x of ['J.pelvis.worldPosition','J.foot_l.worldPosition','J.thigh_l.segmentLength','J.upperarm_l.segmentLength','pelvisLoaded','handsBelowPelvis','kneesForward'])expect(s).toContain(x);expect(s).not.toContain('0.91')});});