import {describe,it,expect} from 'vitest';import fs from 'node:fs';describe('canonical Quaternius rig inspector',()=>{it('extracts hierarchy, world bind transforms and measured axes',()=>{const s=fs.readFileSync('scripts/analyze-quaternius-rig.mjs','utf8');for(const token of ['skinJointCount','worldPosition','worldRotation','primaryWorldAxis','segmentLength','upperarm_l','thigh_l'])expect(s).toContain(token);expect(s).toContain("Expected binary glTF/GLB")});});