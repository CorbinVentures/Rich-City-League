import {describe,it,expect} from 'vitest';import {analyzeRig} from './lab-rig-analysis';
describe('hierarchy-aware rig analysis',()=>{it('propagates parent transforms into world space',()=>{const r=analyzeRig([{name:'pelvis',children:[1],translation:[0,1,0],rotation:[0,0,0,1]},{name:'thigh_l',children:[2],translation:[.2,0,0],rotation:[0,0,0,1]},{name:'calf_l',translation:[0,-.5,0],rotation:[0,0,0,1]}]);expect(r.worldPosition[2].x).toBeCloseTo(.2);expect(r.worldPosition[2].y).toBeCloseTo(.5);expect(r.nodes[2].parent).toBe(1);expect(r.primaryAxis.thigh_l.y).toBeCloseTo(-1)});});