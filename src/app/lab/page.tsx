'use client';

import { FormEvent, useEffect, useState } from 'react';
import { FaArrowRight, FaBolt, FaCheck, FaDumbbell, FaPlay, FaRotate, FaTriangleExclamation, FaChartLine } from 'react-icons/fa6';
import { Container } from '@/components/Container';

const skills = ['Shooting', 'Ball handling', 'Finishing', 'Playmaking', 'Defense', 'Athleticism', 'Rebounding', 'Mental / IQ'];
const levels = ['Beginner', 'Intermediate', 'Advanced', 'Elite']; const lengths = [15, 30, 45, 60, 90];
const environments = ['Indoor court', 'Outdoor court', 'Gym', 'Home / no equipment']; const equipment = ['Basketball', 'Cones', 'Resistance bands', 'Weights', 'Agility ladder', 'None'];
type WorkoutDrill = { id: string; title: string; description: string; prescription: string; duration: number; focus: string[]; coachingPoints: string[]; commonMistakes: string[]; skill: string; videoId: string };
type Workout = { title: string; skill: string; minutes: number; difficulty: string; goal: string; warmup: string[]; drills: WorkoutDrill[]; finisher: string; coachingNotes: string[] }; type ApiError = { message?: string };
function readHistory(): Workout[] { try { const parsed: unknown = JSON.parse(window.localStorage.getItem('rcl-workouts') ?? '[]'); return Array.isArray(parsed) ? parsed.filter((item): item is Workout => Boolean(item) && typeof item === 'object' && typeof (item as Workout).title === 'string') : []; } catch { return []; } }

export default function LabPage() {
  const [skill, setSkill] = useState(skills[0]); const [level, setLevel] = useState(levels[1]); const [length, setLength] = useState(45); const [environment, setEnvironment] = useState(environments[0]); const [selectedEquipment, setSelectedEquipment] = useState<string[]>(['Basketball']); const [goal, setGoal] = useState(''); const [workout, setWorkout] = useState<Workout | null>(null); const [history, setHistory] = useState<Workout[]>([]); const [status, setStatus] = useState<'idle'|'loading'|'error'>('idle'); const [errorMessage, setErrorMessage] = useState('');
  useEffect(() => setHistory(readHistory()), []);
  async function generate(event: FormEvent) { event.preventDefault(); if (status === 'loading') return; if (!selectedEquipment.length) { setStatus('error'); setErrorMessage('Select at least one equipment option.'); return; } setStatus('loading'); setErrorMessage(''); const controller = new AbortController(); const timeout = window.setTimeout(() => controller.abort(), 25000); try { const response = await fetch('/api/lab/workout', { method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify({ skill, level, length, environment, equipment:selectedEquipment, goal:goal.trim() }), signal:controller.signal }); const payload:{workout?:Workout;error?:ApiError|string}=await response.json().catch(()=>({})); if(!response.ok||!payload.workout) throw new Error(typeof payload.error==='string'?payload.error:payload.error?.message||'The session could not be built.'); const next=[payload.workout,...history.filter(item=>item.title!==payload.workout?.title)].slice(0,6); setWorkout(payload.workout);setHistory(next);try{window.localStorage.setItem('rcl-workouts',JSON.stringify(next));}catch{} setStatus('idle'); } catch(error) { setStatus('error'); setErrorMessage(error instanceof Error&&error.name==='AbortError'?'The request took too long. Try again.':error instanceof Error?error.message:'Generation failed. Try again.'); } finally { window.clearTimeout(timeout); } }
  return <main className="min-h-screen bg-[#070b12] pb-24 text-white"><Container maxWidth="xl" className="py-8 sm:py-12">
    <section className="relative overflow-hidden rounded-[2rem] border border-white/10 bg-gradient-to-br from-rcl-navy via-[#0b1422] to-black p-6 shadow-2xl sm:p-10"><div className="absolute -right-24 -top-24 h-72 w-72 rounded-full bg-rcl-orange/15 blur-3xl" /><div className="relative grid gap-8 lg:grid-cols-[1.25fr_.75fr] lg:items-end"><div><div className="flex items-center gap-2 text-[10px] font-black tracking-[.28em] text-rcl-orange"><FaBolt /> RCL PERFORMANCE LAB</div><h1 className="mt-4 font-display text-5xl font-black uppercase leading-[.9] sm:text-7xl">Train with<br /><span className="text-rcl-orange">purpose.</span></h1><p className="mt-5 max-w-2xl text-sm leading-7 text-white/60 sm:text-base">A modern basketball development workspace. Tell us what you want to improve and build a session around your skill, level, time and equipment.</p></div><div className="grid grid-cols-3 gap-2"><Stat icon={<FaChartLine/>} label="FOCUS" value="8 skills"/><Stat icon={<FaDumbbell/>} label="LENGTH" value="15–90m"/><Stat icon={<FaBolt/>} label="COACHING" value="AI guided"/></div></div></section>
    <div className="mt-6 grid gap-6 lg:grid-cols-[.85fr_1.15fr]">
      <form onSubmit={generate} className="rounded-[2rem] border border-white/10 bg-white/[.035] p-5 shadow-xl sm:p-7"><div className="flex items-center justify-between"><div><p className="text-[10px] font-black tracking-[.22em] text-white/35">SESSION BUILDER</p><h2 className="mt-2 font-display text-2xl font-black uppercase">Build your workout</h2></div><span className="rounded-full bg-rcl-orange/10 px-3 py-1 text-[9px] font-black text-rcl-orange">STEP 01</span></div>
        <Field label="What are you developing?"><div className="grid grid-cols-2 gap-2">{skills.map(item=><button type="button" key={item} onClick={()=>setSkill(item)} className={`rounded-xl border p-3 text-left text-xs font-bold transition ${skill===item?'border-rcl-orange bg-rcl-orange/10 text-white':'border-white/10 bg-black/20 text-white/55 hover:border-white/25'}`}>{item}</button>)}</div></Field>
        <Field label="Experience"><div className="grid grid-cols-4 gap-2">{levels.map(item=><button type="button" key={item} onClick={()=>setLevel(item)} className={`rounded-xl border p-2.5 text-[10px] font-black uppercase ${level===item?'border-rcl-orange bg-rcl-orange text-black':'border-white/10 text-white/45'}`}>{item}</button>)}</div></Field>
        <Field label={`Time · ${length} minutes`}><div className="grid grid-cols-5 gap-2">{lengths.map(item=><button type="button" key={item} onClick={()=>setLength(item)} className={`rounded-xl border py-3 text-xs font-black ${length===item?'border-rcl-blue bg-rcl-blue/15 text-white':'border-white/10 text-white/45'}`}>{item}</button>)}</div></Field>
        <Field label="Where are you training?"><select value={environment} onChange={e=>setEnvironment(e.target.value)} className="w-full rounded-xl border border-white/10 bg-black/40 px-4 py-3 text-sm text-white outline-none focus:border-rcl-orange">{environments.map(item=><option key={item}>{item}</option>)}</select></Field>
        <Field label="What do you have with you?"><div className="flex flex-wrap gap-2">{equipment.map(item=><button type="button" key={item} onClick={()=>setSelectedEquipment(current=>current.includes(item)?current.filter(value=>value!==item):[...current,item])} className={`rounded-full border px-3 py-2 text-[10px] font-bold ${selectedEquipment.includes(item)?'border-rcl-gold bg-rcl-gold/10 text-rcl-gold':'border-white/10 text-white/45'}`}>{item}</button>)}</div></Field>
        <Field label="Coach note"><textarea maxLength={500} value={goal} onChange={e=>setGoal(e.target.value)} placeholder="Example: I keep missing left on my three…" className="min-h-24 w-full resize-y rounded-xl border border-white/10 bg-black/40 p-4 text-sm outline-none focus:border-rcl-orange"/></Field>
        <button disabled={status==='loading'} className="mt-5 flex w-full items-center justify-center gap-3 rounded-xl bg-rcl-orange py-4 text-xs font-black uppercase tracking-[.16em] text-black transition hover:bg-white disabled:opacity-50">{status==='loading'?'Building session…':'Generate my workout'}{status==='loading'?<span className="h-4 w-4 animate-spin rounded-full border-2 border-black/30 border-t-black"/>:<FaArrowRight/>}</button>{status==='error'&&<p role="alert" className="mt-4 flex gap-2 text-xs text-red-300"><FaTriangleExclamation/>{errorMessage}</p>}
      </form>
      <section className="min-w-0">{workout?<WorkoutView workout={workout}/>:<div className="flex min-h-[500px] flex-col justify-between rounded-[2rem] border border-dashed border-white/10 bg-gradient-to-b from-white/[.035] to-black/20 p-7"><div><span className="inline-flex rounded-full border border-rcl-blue/30 bg-rcl-blue/10 px-3 py-1 text-[9px] font-black tracking-widest text-rcl-blue">YOUR PERFORMANCE SPACE</span><h2 className="mt-5 max-w-lg font-display text-4xl font-black uppercase">Your next session starts here.</h2><p className="mt-4 max-w-xl text-sm leading-7 text-white/45">Your generated plan will appear here with drills, prescriptions, coaching cues, video demonstrations and a finisher.</p></div><div className="grid gap-3 sm:grid-cols-3"><Mini title="PERSONALIZED" body="Built from your inputs"/><Mini title="COACHING" body="Actionable cues"/><Mini title="TRACKABLE" body="Saved to this device"/></div></div>}
      {history.length>0&&<div className="mt-6 rounded-[2rem] border border-white/10 bg-white/[.025] p-5"><div className="flex items-center justify-between"><div><p className="text-[9px] font-black tracking-[.2em] text-white/35">TRAINING LOG</p><h2 className="mt-1 font-display text-xl font-black uppercase">Recent sessions</h2></div><FaRotate className="text-rcl-blue"/></div><div className="mt-4 grid gap-2 sm:grid-cols-2">{history.slice(0,4).map(item=><button onClick={()=>{setWorkout(item);setStatus('idle')}} key={`${item.title}-${item.minutes}`} className="rounded-xl border border-white/10 bg-black/20 p-4 text-left hover:border-rcl-orange/40"><span className="block text-xs font-black uppercase">{item.skill}</span><span className="mt-1 block text-[10px] text-white/40">{item.minutes} MIN · {item.difficulty}</span></button>)}</div></div>}</section>
    </div>
  </Container></main>;
}
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="mt-6 block"><span className="mb-2 block text-[9px] font-black uppercase tracking-[.2em] text-white/40">{label}</span>{children}</label>}
function Stat({icon,label,value}:{icon:React.ReactNode;label:string;value:string}){return <div className="rounded-2xl border border-white/10 bg-black/25 p-3"><div className="text-rcl-orange">{icon}</div><p className="mt-3 text-[8px] font-black tracking-widest text-white/35">{label}</p><p className="mt-1 text-[11px] font-bold">{value}</p></div>}
function Mini({title,body}:{title:string;body:string}){return <div className="rounded-2xl border border-white/10 bg-black/20 p-4"><p className="text-[9px] font-black tracking-widest text-rcl-gold">{title}</p><p className="mt-2 text-xs text-white/45">{body}</p></div>}
function WorkoutView({workout}:{workout:Workout}){return <article className="overflow-hidden rounded-[2rem] border border-white/10 bg-white/[.035] shadow-2xl"><header className="border-b border-white/10 bg-gradient-to-r from-rcl-navy/70 to-black p-6 sm:p-8"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-[9px] font-black tracking-[.2em] text-rcl-orange">PERSONALIZED SESSION · {workout.skill}</p><h2 className="mt-2 font-display text-3xl font-black uppercase sm:text-4xl">{workout.title}</h2></div><div className="text-right"><strong className="font-display text-4xl text-rcl-orange">{workout.minutes}</strong><span className="block text-[9px] font-black tracking-widest text-white/35">MINUTES</span></div></div><div className="mt-5 rounded-xl border border-white/10 bg-black/20 p-4"><p className="text-[9px] font-black tracking-widest text-rcl-gold">SESSION GOAL</p><p className="mt-2 text-sm text-white/70">{workout.goal}</p></div></header><div className="p-6 sm:p-8"><div className="flex items-center gap-3"><span className="flex h-8 w-8 items-center justify-center rounded-lg bg-rcl-orange text-black font-black">01</span><h3 className="font-display text-xl font-black uppercase">Training plan</h3></div><div className="mt-5 space-y-4">{workout.drills.map((drill,index)=><div key={drill.id} className="rounded-2xl border border-white/10 bg-black/20 p-5"><div className="flex gap-4"><span className="text-lg font-black text-white/20">{String(index+1).padStart(2,'0')}</span><div className="min-w-0 flex-1"><div className="flex flex-wrap justify-between gap-3"><h4 className="font-display text-lg font-black uppercase">{drill.title}</h4><span className="rounded-full bg-rcl-orange/10 px-3 py-1 text-[9px] font-black text-rcl-orange">{drill.prescription}</span></div><p className="mt-2 text-sm leading-6 text-white/55">{drill.description}</p><div className="mt-3 flex flex-wrap gap-2">{drill.focus.map(item=><span key={item} className="rounded-full border border-white/10 px-2 py-1 text-[9px] text-white/45">{item}</span>)}</div><details className="mt-4"><summary className="cursor-pointer text-xs font-bold text-white/70">Coaching cues & common mistakes</summary><ul className="mt-3 space-y-2 text-xs text-white/50">{drill.coachingPoints.map(item=><li key={item}>• {item}</li>)}</ul><p className="mt-3 text-xs text-white/40"><strong className="text-white/60">Avoid:</strong> {drill.commonMistakes.join(' · ')}</p></details><div className="mt-5 overflow-hidden rounded-xl border border-white/10 bg-black"><div className="relative aspect-video"><iframe title={`${drill.title} demonstration`} src={`https://www.youtube.com/embed/${drill.videoId}`} loading="lazy" className="absolute inset-0 h-full w-full" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowFullScreen/></div><p className="flex items-center gap-2 px-3 py-2 text-[9px] font-black tracking-widest text-rcl-orange"><FaPlay/> DEMONSTRATION · {drill.skill}</p></div></div></div></div>)}</div><div className="mt-6 grid gap-4 sm:grid-cols-2"><div className="rounded-2xl border border-rcl-orange/20 bg-rcl-orange/5 p-5"><p className="text-[9px] font-black tracking-widest text-rcl-orange">FINISHER</p><p className="mt-2 text-sm text-white/70">{workout.finisher}</p></div><div className="rounded-2xl border border-white/10 bg-white/[.02] p-5"><p className="text-[9px] font-black tracking-widest text-rcl-gold">COACHING NOTES</p><ul className="mt-2 space-y-2 text-xs text-white/50">{workout.coachingNotes.map(note=><li key={note} className="flex gap-2"><FaCheck className="mt-0.5 text-rcl-orange"/>{note}</li>)}</ul></div></div></div></article>}
