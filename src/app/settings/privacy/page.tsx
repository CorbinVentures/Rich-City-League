'use client';
import Link from 'next/link'; import { Container } from '@/components/Container'; import { useAuth } from '@/hooks/useAuth'; import { useState } from 'react';
export default function PrivacyCenter(){const {user}=useAuth();const [sent,setSent]=useState(false);return <main className="min-h-screen bg-rcl-black text-white"><Container maxWidth="lg" className="py-14"><p className="text-xs font-black tracking-[.25em] text-rcl-orange">ACCOUNT CONTROL</p><h1 className="mt-2 font-display text-5xl font-black">PRIVACY CENTER</h1><p className="mt-4 max-w-2xl text-white/60">Review RCL privacy information and start an account-data request.</p><div className="mt-8 grid gap-4"><Link href="/legal/privacy" className="rounded-2xl border border-white/10 p-5 font-bold">Read Privacy Policy →</Link><section className="rounded-2xl border border-white/10 p-5"><h2 className="font-bold">Access, correction or deletion</h2><p className="mt-2 text-sm text-white/55">For safety, requests must be authenticated. Until automated export/deletion is deployed, submit a request through RCL’s published support/contact channel from the email associated with your account. RCL may retain records where permitted or required.</p>{user?<button onClick={()=>setSent(true)} className="mt-4 rounded-xl bg-rcl-gold px-4 py-3 font-bold text-black">{sent?'Request instructions acknowledged':'Start request'}</button>:<Link href="/auth/sign-in" className="mt-4 inline-block text-rcl-gold">Sign in to continue →</Link>}</section></div></Container></main>}